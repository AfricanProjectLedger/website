const menu=document.querySelector('.menu-btn');const nav=document.querySelector('.navlinks');if(menu&&nav){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))})}document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());document.querySelectorAll('form[data-demo]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const email=form.querySelector('input[type="email"]')?.value||'';const name=form.querySelector('input:not([type])')?.value||'';const message=form.querySelector('textarea')?.value||'Please add me to the APL founding access list.';const subject=form.classList.contains('form-grid')?'APL website enquiry':'APL founding access request';location.href=`mailto:research@africanprojectledger.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(`Name: ${name}\nEmail: ${email}\n\n${message}`)}`}));
